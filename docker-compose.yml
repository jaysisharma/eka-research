version: '3.8'

services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY}/eka-research-app:latest
    container_name: eka_app
    restart: always
    environment:
      - NODE_ENV=production
    networks:
      - shared_network

networks:
  shared_network:
    external: true